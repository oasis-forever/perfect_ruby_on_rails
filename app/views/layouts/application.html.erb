<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html" charset="utf-8" http-equiv="content-type">
    <title>パーフェクトRuby on Rails</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="header navbar navbar-expand-sm navbar-light bg-light">
      <div class="container">
        <%= link_to 'イベント一覧', root_path, class: 'navbar-brand mr-auto' %>
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= link_to '新規イベント登録', new_event_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <% if logged_in? %>
              <%= link_to 'ログアウト', logout_path, class: 'nav-link', method: :delete %>
            <% else %>
              <%= link_to 'GitHubでログイン', '/auth/github', class: 'nav-link', method: :post %>
            <% end %>
          </li>
        </ul>
      </div>
    </header>
    <div class="container">
      <% if flash.notice %>
        <div class="alert alert-success">
          <%= flash.notice %>
        </div>
      <% end %>
      <% if flash.alert %>
        <div class="alert alert-danger">
          <%= flash.alert %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
